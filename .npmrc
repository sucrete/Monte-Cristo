# Suppress deprecated npm config warnings
# These warnings appear in npm 11.x for legacy config options
loglevel=error

# sanity-plugin-bulk-actions-table declares a strict sanity peer range that
# conflicts with the installed sanity version; install with legacy resolution.
legacy-peer-deps=true

